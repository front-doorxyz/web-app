import { useEffect, useRef, useState } from "react";

function Client({  }) {

    return (
        <div className="customer container main-component">
          <div className="row">
            <h2>Client page...</h2>
          </div>
        </div>
        
    );
}

export default Client;
